export interface MenuContext {
    $mid?: number;
    fspath?: string;
    external?: string;
    path: string;
    scheme: string;
}
export interface TreeItemOptions {
    schname: string;
    rulename: string;
}